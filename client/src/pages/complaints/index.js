import { useEffect, useState } from 'react';
import Link from 'next/link';
import { api } from '../../services/api';

export default function Complaints() { const [complaints, setComplaints] = useState([]); const [error, setError] = useState(''); useEffect(() => { api.get('/complaints').then(({ data }) => setComplaints(data.complaints)).catch(() => setError('Please sign in to view your complaints.')); }, []); return <main className="min-h-screen px-6 py-10"><div className="mx-auto max-w-5xl"><Link href="/dashboard" className="text-sm font-bold">← Dashboard</Link><div className="mt-14 flex items-end justify-between"><div><p className="text-sm font-bold uppercase tracking-widest text-coral">Complaint history</p><h1 className="mt-3 text-5xl font-bold">Your reports</h1></div><Link href="/complaints/new" className="rounded-full bg-coral px-5 py-3 font-bold">New report</Link></div>{error && <p className="mt-8 text-coral">{error}</p>}<div className="mt-10 space-y-3">{complaints.map((complaint) => <Link key={complaint._id} href={`/complaints/${complaint._id}`} className="block rounded-2xl bg-white p-5 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-widest text-ink/50">{complaint.ticketId}</p><h2 className="mt-1 text-xl font-bold">{complaint.title}</h2></div><span className="rounded-full bg-mint px-3 py-1 text-sm font-bold">{complaint.status}</span></div><p className="mt-3 text-sm text-ink/60">{complaint.category} · {complaint.priority} priority</p></Link>)}{!error && complaints.length === 0 && <p className="rounded-2xl bg-white p-8 text-ink/60">No complaints yet.</p>}</div></div></main>; }
